replicaCount: 1

image:
  repository: abhishek070901/go-web-app:latest
  pullPolicy: IfNotPresent
  tag: "21902055978"

ingress:
  enabled: false
  className: ""  # set your ingress class, e.g., "nginx" if needed
  annotations: {}  # empty object if no annotations
  # kubernetes.io/ingress.class: nginx
  # kubernetes.io/tls-acme: "true"
  hosts:
    - host: chart-example.local
      paths:
        - path: /
          pathType: ImplementationSpecific